---
title: "Detection evaluation"
description: "Measure case-level precision, recall, coverage and reproducibility with pinned external labels."
icon: "chart-no-axes-combined"
---

Use several evaluation layers: owned regression fixtures for changed behavior,
public labeled corpora for broader diagnostics, and independently reviewed
application cases for release decisions. A high score on one layer does not
establish commercial detection accuracy.

## Owned finding-location evaluation

`aegify benchmark` evaluates a version 1 ground-truth manifest against an isolated
local source tree. The manifest declares the rule scope and expected file/line
locations; the checked-in core corpus is a small regression contract. The output
is **report schema version 2**. Consumers of older reports must allow `null`
precision, recall and F1 when the corresponding denominator is empty.

```bash
cd scanner
uv run aegify benchmark benchmarks/core-v1/sources \
  --ground-truth benchmarks/core-v1/ground-truth.json \
  --min-precision 1.0 --min-recall 1.0 \
  --output-file /path/to/reports/core.json
```

The runner uses bundled rules, memory storage, sequential parsing, no output
caps and a 50,000-context taint budget. It ignores repository configuration and
`AEGIFY_*` environment overrides. Its explicit empty exclusion list includes owned
test fixtures. Unsupported inputs or parser/resource gaps still make analysis
incomplete. No provider, corpus dependencies or application code is invoked.

Source, auxiliary files, label bytes, effective configuration and implementation
must remain unchanged during analysis. Labels must reference existing source
lines; missing executed rules or analyzed labeled files produce an incomplete
evaluation. The report keeps observed matching metrics for diagnosis alongside
`evaluation_complete`, `missing_rules`, `missing_files` and analysis gaps.
Only complete evaluations with defined ratios can pass a quality threshold.

Keep output outside the source directory and separate from the label file.
Single-file targets bind their complete enclosing directory, including auxiliary
inputs. Use an isolated directory: inventory is bounded to 50,000 entries, 8 MiB
per file and 256 MiB overall; parsing remains bounded to 1 MiB per source file.
Links and special files are rejected. Artifact reads require a POSIX host
(Linux/macOS): they use non-following, nonblocking opens and validate the opened
descriptor's type and size. Unsupported hosts fail before evaluation. Reports
are published atomically. Evaluate an isolated, immutable checkout; before/after
hashes detect observed changes and are not a substitute for filesystem isolation.

Version 1 manifests remain valid. Earlier use of repository settings, environment
overrides, output inside the corpus or coercible JSON values requires migration
to these explicit inputs. The UTF-8 source digest uses byte lengths; non-ASCII
path digests from the older implementation require a new baseline. CI runs the
core evaluation in two separate processes and compares source/label/outcome and
implementation/configuration identities. This proves repeatability only within
that owned regression scope.

## OWASP Python case evaluation

`aegify benchmark-owasp` reads a local
[OWASP Benchmark Python](https://github.com/OWASP-Benchmark/BenchmarkPython)
source tree and its expected-results CSV. It uses the common static scanner,
bundled rules, low severity threshold, serial parsing and 50,000 taint contexts.
The output caps are disabled. All Python files are selected, including
helpers; the scanner also reads auxiliary source-analysis inputs such as OpenAPI.

The runner ignores corpus `.aegify.yml` and `AEGIFY_*` configuration overrides.
It uses memory storage, makes no LLM calls, installs no corpus dependencies and
does not execute the benchmark application or scripts. The corpus is hashed
before and after the scan, including auxiliary files, and changes invalidate the
evaluation. Symbolic links, oversized inputs and malformed labels are rejected.

```bash
cd scanner
uv run aegify benchmark-owasp /path/to/BenchmarkPython \
  --expected-results /path/to/BenchmarkPython/expectedresults-0.1.csv \
  --output-file /path/to/reports/owasp-python.json \
  --min-precision 0.90 --min-recall 0.90
```

Keep the output outside the corpus tree. Use an exact upstream commit and verify
the archive digest before extracting it. The checked-in baseline and replay
instructions are in
[`scanner/benchmarks/owasp-python-v01`](https://github.com/Aeg1sx/Aegify/tree/main/scanner/benchmarks/owasp-python-v01).
The adapter currently supports the Python `testcode/BenchmarkTestNNNNN.py`
layout. Java and other benchmark formats need their own adapters.

### Cookie-option regression, 2026-09-24

The structural cookie-option change is recorded in
[`scanner/benchmarks/cookie-options-v1`](https://github.com/Aeg1sx/Aegify/tree/main/scanner/benchmarks/cookie-options-v1).
It uses the same pinned source, official labels, matching policy and configuration
as the preceding hash-selection baseline. The original frozen reports remain
available.

| All-candidate cases | Before | After |
|---|---:|---:|
| True positives | 209 | 209 |
| False positives | 114 | 99 |
| False negatives | 225 | 225 |
| True negatives | 645 | 660 |
| Precision | 64.71% | 67.86% |
| Recall | 48.16% | 48.16% |
| Accuracy | 71.58% | 72.84% |

The 15 changed labels are CWE-614 negatives with literal `secure=True`; the
updated selector retains all 24 positive cases. Other-CWE and blocking-channel
metrics are unchanged. All 1,236 Python files were analyzed without a reported
gap; 37 CWE-501 cases remain unscored. This diagnostic baseline still exits 3
and does not pass the release gate. The 39 public cookie cases measure a narrow
selection regression, not general cookie detection or held-out AI quality.

## What gets counted

Each label identifies one case, intended CWE and positive/negative outcome.
An Aegify finding counts only when its relative case path and CWE both match,
and its rule's declared CWE agrees with the finding and appears in the scan's
executed-rule inventory. No parent/child CWE aliases are applied implicitly.

| Label | Matching finding | Case outcome |
|---|---|---|
| Positive | Present | True positive |
| Positive | Absent | False negative |
| Negative | Present | False positive |
| Negative | Absent | True negative |

Duplicate findings in one case count once. Results are reported separately for
all candidates, advisory findings and blocking findings. `--blocking-only`
applies the CLI quality thresholds to the blocking channel; it does not remove
the other channels from the report. A blocking disposition is a scanner gate
decision, not a runtime impact claim.

These are **case-level metrics for the intended CWE**, not finding-level
precision across all emitted alerts. A finding for a different CWE, or in a
helper outside the labeled cases, remains an unscored observation. The upstream
labels do not exhaustively label every possible issue in each file. Unscored
observations need separate review; they cannot all be called false positives.
The report retains their counts and provides each case's matched rules and
number of observations for diagnosis.

## Coverage and abstention

A case is unscored if its file was not analyzed or no executed rule declares its
CWE. An analysis failure, syntax recovery, truncation or another global gap makes
all cases unscored: current scan diagnostics cannot reliably attribute every gap
to individual cases. Observed matches are retained for investigation.

Unscored cases never become true negatives. Reports include the scored fraction,
reasons for abstention and recall with **all positive labels** in the denominator.
The ordinary precision and recall fields describe only the scored subset.
The presence of an executed CWE rule does not imply support for every API,
framework or data-flow construct represented in that category.

| Exit | Meaning |
|---:|---|
| `0` | Every case scored, defined precision/recall, requested thresholds passed |
| `1` | Complete evaluation with undefined precision/recall or a failed quality threshold |
| `2` | Invalid/changed input, report write failure or scanner failure |
| `3` | Incomplete analysis or any unscored case; a diagnostic report is retained |

Fix the coverage or model gap before treating exit `3` as a passing quality gate.
Do not lower quality thresholds to describe the current diagnostic baseline as
release acceptance.

## Metrics and replay evidence

Reports include TP/FP/FN/TN, precision, recall, F1, accuracy, balanced accuracy,
false-positive rate and Matthews correlation coefficient. An undefined ratio is
`null`, never an invented perfect score. Per-CWE tables retain every category;
per-rule tables evaluate all cases for the rule's exact CWE.

Wilson 95% intervals are included for precision and recall as descriptive
binomial intervals. They assume independent cases and do not account for shared
templates, training contamination, model selection or differences between this
corpus and production applications. Public benchmark cases are not a held-out
AI test set. This adapter is not the official BenchmarkUtils scorecard generator.

The report records corpus and label hashes, scanner code/modelpack and rule
hashes, parser fingerprint, installed package versions, effective configuration,
executed rule IDs and a canonical case-outcome digest. Compare the outcome digest
across separate processes; scan UUIDs, timing and process memory are not part of
that digest. A changed corpus, implementation or configuration requires a new
baseline.

Timing includes scan time and complete evaluation time. Process and child RSS
high-water marks are separate and must not be summed as simultaneous memory
usage. Individual developer-machine runs are not p50/p95 latency guarantees.

## Compare saved OWASP reports

Use `aegify compare-owasp` to compare full JSON reports without rescanning or
executing their source. The comparison supports the exact-case/CWE v1 adapter.

```bash
aegify compare-owasp baseline.json candidate.json \
  --output-file comparison.json

aegify compare-owasp first-run.json repeat-run.json \
  --require-identical --output-file replay-comparison.json
```

The loader recomputes aggregate, per-CWE and per-rule metrics from every case;
checks the outcome/configuration digests, identities, health, counts, matching
rules and abstentions; and rejects ambiguous or non-finite JSON. Source, label,
configuration and case identities must match between reports. It does not trust
an aggregate score or an embedded claim of successful evaluation.

The default gate rejects **any** correct-to-incorrect case transition in any
channel, even when another case improves enough to hide the regression in an
aggregate metric. Losing scored coverage also fails. Metrics are compared on the
jointly scored cases; coverage gains/losses and all changed case evidence remain
visible separately. No statistical significance is inferred from correlated
synthetic templates.

`--require-identical` additionally requires equal implementation, dependency,
parser, environment and executed-rule identities, matching observations/counts,
analysis health and snapshot. Timing and process RSS are excluded. Use reports
from separately executed processes; comparing one report with itself only checks
artifact consistency.

Archived reports with separate CSV files require explicit `--baseline-cases` and
`--candidate-cases` paths. Both historical `|` and `;` rule separators are supported;
the reconstructed cases must reproduce the JSON outcome digest. The command never
discovers or downloads a companion file from a report. Embedded cases and an
external CSV cannot be supplied together. Inputs are bounded to 32 MiB JSON,
16 MiB CSV and 50,000 cases; output cannot replace an input artifact.

| Exit | Comparison result |
|---:|---|
| `0` | Complete reports and selected comparison gate passed |
| `1` | A case/coverage regression, or a requested replay mismatch |
| `2` | Invalid/incomparable input, a failed scanner run, or report write failure |
| `3` | Gate has no detected regression/mismatch, but at least one evaluation is incomplete |

A comparison passing its relative gate does not establish an absolute quality
threshold. Preserve exit `3` for the public baseline's unscored category. Artifact
hashes identify supplied data and do not authenticate its author or independently
adjudicate labels. Detection quality remains dependent on the workload and
weakness classes, as discussed in [NIST SATE VI](https://doi.org/10.6028/NIST.SP.500-341).

## Release evaluation

Before setting a production gate:

1. Declare supported language/framework/CWE combinations and unsupported scope.
2. Keep application-family and commit splits separate between calibration and
   evaluation; review labels without using the scanner's prediction as truth.
3. Review false positives and misses with source-bound evidence and a second
   adjudication. Preserve disagreements and uncertain labels.
4. Measure findings per review hour, duplication, time to triage, escaped defects,
   CI false-block rate and operating cost alongside detection metrics.
5. Repeat on fixed hardware and frozen inputs; compare incremental and full scans.
6. Evaluate AI review on a separate labeled set with repeated model runs, explicit
   abstention, source-citation validity, provider/model versions and token costs.

Juice Shop and DVWA coverage checks remain separate from this labeled evaluation.
In particular, a scan of DVWA JavaScript utilities does not measure detection in
its unsupported PHP backend.
