---
title: "AI provider configuration"
description: "Native protocols, compatible gateways, explicit model IDs, and credential isolation."
---

## Protocols and services

Settings → AI review supports four implemented wire protocols. Service presets set
an endpoint and protocol; they do not claim a separate native adapter for every vendor.

| Protocol | Operation | Authentication on the default host |
| --- | --- | --- |
| Anthropic Messages | `/v1/messages` | Stored Anthropic key |
| OpenAI Chat Completions | `/v1/chat/completions` | Stored OpenAI key |
| OpenAI Responses | `/v1/responses` | Stored OpenAI key |
| Google Gemini | `/v1beta/models/MODEL:generateContent` | Stored Google key |

Presets cover Anthropic, OpenAI Responses/Chat, Google Gemini, Azure OpenAI v1,
OpenRouter, Groq, and a custom HTTPS gateway. Azure uses the deployment name as
the model ID and an explicit `api-key` custom header. OpenRouter and Groq use a
dedicated `Authorization: Bearer ...` custom header. Do not paste an API key into
the endpoint field. Legacy Azure endpoints that require a dated `api-version`
query are not part of the Azure v1 preset.

Choose an exact model identifier available to your account. The UI does not guess
a "latest" model, silently replace a model on provider change, or assert that a
particular model is enabled for your account. Set the request timeout, output token
budget, and—when using Chat Completions—the token parameter accepted by the model:
`max_tokens` or `max_completion_tokens`.

API origins, versioned base paths, and complete operation URLs are supported without
duplicating `/v1`. The request URL preview shows the resulting URL before saving.
OpenAI Responses requests use `store: false`. This is not a guarantee about all
provider retention policies. Tool calls and thought parts are not executed or
treated as answer text. No new autonomous tool-execution workflow is enabled.

## Credentials and egress

Direct-provider keys are only attached to the built-in default endpoints. Any custom
endpoint—including a preset vendor endpoint—uses only explicitly entered custom
headers. Those headers are encrypted and are never returned to the browser, even
for legacy plaintext records. Changing endpoint/protocol requires explicitly
replacing or removing existing custom headers to avoid forwarding a credential to
another service. Blank untouched secret fields preserve their stored values.

Requests require HTTPS, public DNS addresses, certificate verification, and a
connection pinned to the validated DNS result. Redirects, private/loopback endpoints,
credential-bearing URLs, and unbounded responses are rejected. The transport caps
responses at 2 MiB and does not retry automatically. Use an approved public HTTPS
gateway for a private model deployment; direct local Ollama access is not enabled.
Bedrock SigV4, Vertex service-account authentication, and native local-model
protocols are not advertised as implemented adapters.

Saving validates configuration; it does not contact an external service. "Test
saved connection" asks for confirmation and sends a small structured-response
probe using saved settings. This can incur usage. Unit tests use mocked provider
responses, and do not establish real credentials, quota, or model availability.

References: [OpenAI Responses](https://developers.openai.com/api/reference/cli/resources/responses/methods/create),
[Gemini generateContent](https://ai.google.dev/api/generate-content),
[Azure OpenAI v1](https://learn.microsoft.com/en-us/rest/api/microsoft-foundry/azureopenai/chat),
[OpenRouter](https://openrouter.ai/docs/api_reference/overview), and
[Groq compatibility](https://console.groq.com/docs/openai).

## Durable dashboard review receipts

Queued finding reviews use a separate [AI worker](/operations/self-hosted-workers).
They require an explicit normal completion (`end_turn`, `STOP`, completed Responses
messages, or Chat `stop`) and a bounded review schema. Unexpected tools, refusals,
truncated output and missing completion signals fail the batch, with no automatic
provider retry. This receipt contract applies to queued finding reviews; other
direct connection/analysis paths retain their existing request interfaces.

Call receipts retain response/request digests and allowlisted numeric usage
counters, using the provider's native field names. Anthropic cache read/creation
counters are separate from its `input_tokens`; OpenAI and Gemini counters have
different inclusion rules. Missing counters and monetary cost remain unknown.
Reservations are upper bounds, not billed usage. Live provider compatibility,
pricing-version accounting and review-quality calibration remain separate checks.

Completion and usage references: [Anthropic Messages](https://platform.claude.com/docs/en/api/messages/create),
[Anthropic stop reasons](https://platform.claude.com/docs/en/build-with-claude/handling-stop-reasons),
[OpenAI Responses](https://developers.openai.com/api/reference/cli/resources/responses/methods/create),
and [Gemini generateContent](https://ai.google.dev/api/generate-content).

## Scanner agent adapters

`aegify agent-run` has its own scanner-side OpenAI, Anthropic and CLI adapters.
These are separate from the dashboard's provider settings and transport above.

### Scanner Anthropic calls and accounting

`scan --llm`, `scan-pr --llm`, `scan-workspace --ai-tools` and
`agent-run --provider anthropic-api` share the scanner's Anthropic client.
It reserves a call before invoking the SDK, disables SDK retries and redirects,
and bounds success and error bodies before decoding. A response must contain an
assistant message, provider response ID/model and `stop_reason: end_turn`.
Text blocks are joined; thought blocks are ignored. Tools, refusals, truncated
output, duplicate JSON keys, nonfinite numbers and Markdown-wrapped JSON do not
produce a review. The agent adapter additionally requires all four narrative fields.

| Limit | Scanner Anthropic behavior |
| --- | --- |
| Request size | At most 262,144 encoded JSON bytes per call; oversized requests are rejected |
| Cumulative request bytes | At most 4,000,000 bytes per client budget |
| Output request | 1–32,768 tokens per call; at most 655,360 requested across calls |
| Call count | `llm.max_calls`, default 100, maximum 250 |
| Response size | 2 MiB, including unsuccessful HTTP responses; compressed replies are rejected |
| Timeout | 5-second connect and 10-second I/O timeouts; 60-second elapsed bound checked between body reads |

The elapsed bound does not interrupt an OS/DNS stall or replace an external job
deadline. The SDK's operator-configured endpoint/proxy behavior remains in use;
this client does not inherit the dashboard's public-DNS validation and connection
pinning. Operators must restrict the scanner worker's allowed providers and egress.

`token_usage` in scanner JSON, `tokenUsage` in SARIF invocation properties, and
`token_usage` in Anthropic agent artifacts retain call IDs, phases, requested and
returned model IDs, request/complete-response digests, stop/error codes, elapsed
time and native usage. Raw prompts, provider bodies and hidden reasoning are not
copied into these receipts. SDK body-debug logging is disabled even with scanner
`--verbose`; Aegify logs bounded call lifecycle events instead.

Input estimation uses encoded request bytes divided by four, rounded up, plus
the full requested output allowance. This is admission control, not an accurate
tokenizer or invoice ceiling. Incomplete usage keeps the unaccounted reservation.
Native `input_tokens`, `output_tokens`, `cache_creation_input_tokens` and
`cache_read_input_tokens` are counted once; overlapping subdivisions are excluded.
Missing, negative, boolean or otherwise invalid counters never become trusted zero
usage. A useful complete narrative may coexist with unknown usage.

Attempted calls have `total_cost_usd: null` and `cost_status: unavailable`, including
when reported tokens are zero. Only a budget with no call has zero cost. Old numeric
costs remain readable as `legacy_estimate` and are labeled unverified. CLI and both
GitHub report formats show this uncertainty even when there are no findings.
No provider tariff or billing reconciliation is implemented here. Other agent
providers expose `token_usage: null`; that does not mean they were free.

Configuration example:

```yaml
llm:
  token_budget: 100000
  max_calls: 100
  batch_size: 5
  max_retries: 0
```

The previously unused nonzero `max_retries` setting now fails validation; remove
it or set it to zero. Consumers of old scanner JSON must accept nullable cost and
inspect the accompanying status. Receipt metadata is retained when the CLI writes
its artifact; this is not a durable crash journal for a killed CLI process.

References: [Anthropic Python SDK](https://platform.claude.com/docs/en/cli-sdks-libraries/sdks/python)
and [stop reasons](https://platform.claude.com/docs/en/build-with-claude/handling-stop-reasons).

### Narrative and process contracts

The OpenAI Responses adapter requests strict structured output with all four
narrative fields required: `summary`, `claims`, `evidence_gaps`, and
`recommendations`. Empty lists must be present. The Codex output schema uses the
same contract. Local artifact models retain defaults so older saved narratives
can still be read. New OpenAI, Codex and Claude Code responses must contain all
four fields, without extra keys, duplicate keys, non-JSON numbers or Markdown
wrappers. Pydantic's text/list limits remain enforced locally.

OpenAI results need a completed response and one completed assistant message.
Refusals, provider errors, incomplete output, unexpected tool calls, multiple
messages and inconsistent status fields do not produce a narrative. Reasoning
items are ignored, not used as answer text. Requests retain `store: false`, a
4,096-token output limit and a 2,000,000-byte response cap. Redirects are rejected.
HTTP/provider errors retain a bounded diagnostic without copying remote bodies.
The socket timeout is not a durable job deadline or a provider-wide spend budget.

Claude Code requires a successful result envelope, `is_error: false`, no API error,
and an absent or completed stop reason. A refused or unfinished result is rejected
even if its text happens to contain a complete JSON object. Codex must exit
successfully and supply the complete message file; it does not yet provide a
separate provider-status/usage receipt to Aegify.

| `backend_error_code` | Meaning |
| --- | --- |
| `refused` | The provider declined the review |
| `incomplete` | The provider did not finish its response |
| `invalid_response` | The response shape, encoding or message file was invalid |
| `response_limit` | Output exceeded the configured byte bound |
| `provider_error` | The provider failed or returned an unsuccessful envelope |
| `timeout` | API socket or CLI invocation timeout |
| `process_error` | CLI launch, I/O or exit failure |
| `unsupported_platform` | CLI process controls are unavailable on this platform |
| `incomplete_response` | Scanner Anthropic response did not end normally |
| `budget_exhausted` | Scanner Anthropic call/request allowance was exhausted |
| `provider_transport_error` | Scanner Anthropic HTTP or transport error; billing remains unknown |
| `transport_timeout` / `body_deadline_exceeded` | Scanner Anthropic I/O or observed body deadline |
| `response_too_large` | Scanner Anthropic response exceeded its byte bound |

The new field defaults to an empty string for historical artifacts and failures
outside these adapters. Failed model enrichment leaves static evidence intact.
Empty failure codes are omitted from the artifact digest to preserve historical
v1 digests; a nonempty failure code is included. Absent accounting preserves those
digests too; actual Anthropic call receipts are included in new artifact digests.
`agent-run` writes the retained artifact before exiting: `0` means completed,
`3` means partial or awaiting approval/evidence, and `2` means failed input,
configuration or execution. Previously a partial review could exit `0`; CI
consumers must handle `3` explicitly and must not equate it with a completed review.
Offline tests cover strict schemas, refusal/incomplete/error envelopes, malformed
JSON, output flooding, a blocked input pipe, deadlines, owned child cleanup and
message-file limits. They invoke no provider and execute no analyzed source.
POSIX process groups cannot contain a program that deliberately detaches; dedicated
worker/container isolation, provider usage accounting, cancellation and durable
job recovery remain required for a multi-tenant installation.

Protocol references: [OpenAI Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs),
[Codex configuration](https://learn.chatgpt.com/docs/config-file/config-reference),
[Claude Code programmatic use](https://code.claude.com/docs/en/headless), and
[Claude result messages](https://code.claude.com/docs/en/agent-sdk/typescript#sdkresultmessage).
