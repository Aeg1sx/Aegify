# The root build context is used only by scanner/Dockerfile. Keep the context
# allowlisted so local credentials, Git metadata, dashboards, and evidence can
# never be copied into a scanner build layer.
**
!scanner/
scanner/**
!scanner/pyproject.toml
!scanner/uv.lock
!scanner/README.md
!scanner/hatch_build.py
!scanner/src/
!scanner/src/**
!rules/
!rules/**
